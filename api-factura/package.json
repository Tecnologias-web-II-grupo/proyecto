{
  "name": "api-factura",
  "version": "4.2.0",
  "description": "Factura Bonita: portal React para registrar ventas, cobrar y generar facturas personalizadas.",
  "main": "src/server.js",
  "scripts": {
    "test": "node --test --experimental-test-isolation=none",
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "postinstall": "node scripts/installChrome.js && npm --prefix frontend install && npm --prefix frontend run build",
    "install:chrome": "node scripts/installChrome.js",
    "build:frontend": "npm --prefix frontend install && npm --prefix frontend run build",
    "dev:frontend": "npm --prefix frontend run dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mysql2": "^3.23.2",
    "puppeteer": "^25.7.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "multer": "^2.0.2"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
